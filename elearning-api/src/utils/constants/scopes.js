const GOAL_SCOPES = Object.freeze({
    GLOBAL: 'GLOBAL',
    DEPARTMENT: 'DEPARTMENT',
    USER: 'USER'
});

const ANNOUNCEMENT_SCOPES = Object.freeze({
    GLOBAL: 'GLOBAL',
    DEPARTMENT: 'DEPARTMENT'
});

module.exports = {
    GOAL_SCOPES,
    ANNOUNCEMENT_SCOPES
};
